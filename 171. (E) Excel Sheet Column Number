class Solution {
public:
    int titleToNumber(string s) {
        int sum = 0, l = s.size();
        for(int i = 0; i < l; i ++) {
            sum = sum + (s[i] - 'A' + 1) * pow(26, l-i-1);
        }
        return sum;
    }
};

@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@


